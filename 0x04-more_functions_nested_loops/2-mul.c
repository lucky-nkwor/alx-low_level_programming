#include "main.h"
/**
 * mul - multiple two integers
 * @a: the first interger to be multipled
 * @b: the second integer to be multiplied
 * Return: the result of the multiplication
 */
int mul(int a, int b)
{
	return (a * b);
}
